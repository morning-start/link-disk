[package]
name = "link-disk"
version = "1.0.0"
edition = "2024"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
toml = "0.8"
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0"
dirs = "5.0"
spinners = "4.1"

[profile.release]
strip = true
opt-level = "z"
